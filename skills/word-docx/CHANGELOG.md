# Changelog

## [2.0.0] - 2026-06-04

### 角色调整

- 「{{USER_SHORT_NAME}}默认排版配置」章节从"标准配置"调整为"**无模板时的回退配置**"。
- SKILL.md 加显式提示：**给了模板时以模板样式为准**，本配置仅在 `docx` 技能走无模板路径（python-docx + `styles.py`）时生效。
- 这一变更与 `docx` 技能 v2.1.0 引入的双路径写入架构对齐：双路径下"模板优先"是默认行为，本配置只决定"没给模板时按什么样式兜底"。

## [1.0.0] - 2026-05-31
### 新增
- 对齐 SKILL-DEV-GUIDE 书写规范，补全 Frontmatter（author/version/license/负向条件）
